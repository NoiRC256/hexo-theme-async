<!-- menu -->
<div class="trm-menu">
    <nav>
        <ul>
            <% theme.top_bars.forEach(element=> { %>
            <li class="menu-item-has-children <%= url_for_lang(element.url) + 'index.html' === url_for_lang(path) ?'current-menu-item':'' %>">
                <a <%- element.noswup ? "data-no-swup":"" %> href="<%- url_for_lang(element.url) %>" target="<%- element.target %>">
                    <%-__(element.title)%>
                </a>
                <% if(element.children) { %>
                <ul>
                    <% element.children.forEach(item=> { %>
                    <li>
                        <a <%- item.noswup ? "data-no-swup":"" %> href="<%- url_for_lang(item.url) %>" target="<%- item.target %>">
                            <%-__(item.title)%>
                        </a>
                    </li>
                    <% }) %>
                </ul>
                <% } %>
            </li>
            <% }) %>
            <%- partial(theme.layout.header_lang_switcher) %>
        </ul>
    </nav>
</div>
<!-- menu end -->